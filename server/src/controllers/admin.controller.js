import Problem from '../models/Problem.model.js';

const create = async(req,res)=>{
    try{

    }
    catch(error){
        res.status(400).json({
            success : false,
            error : {
                message : error.
            }
        })
    }
}